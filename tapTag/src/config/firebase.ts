/*
  Demo branch note:
  The app runtime on this branch does not use Firebase. Data and auth live in
  local AsyncStorage-backed services so a tester can clone and run the project
  without backend setup.

  This file remains only as a marker for the architectural shift from the main
  Firebase-backed branch.
*/

export const app = null;
export const auth = null;
export const db = null;
